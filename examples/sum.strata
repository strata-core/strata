// Example: while loop to sum numbers 1 to N
fn sum_to(n: Int) -> Int {
    let mut total = 0;
    let mut i = 1;
    while i <= n {
        total = total + i;
        i = i + 1;
    };
    total
}

let result = sum_to(10);
