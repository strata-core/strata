	•	Unit tests in each crate (types: effect-row algebra; parse: golden files).
	•	Snapshot tests for traces/logic later (lock formats early).
	•	Property tests for actor mailbox invariants (when implemented).
	•	Determinism test harness reserved name: replay_*.rs.